import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// Production config für Deployment auf node0
// Proxies werden von nginx übernommen
export default defineConfig({
  plugins: [vue()],
  build: {
    outDir: 'dist',
    sourcemap: false
  },
  // Base URL für Production
  base: '/'
})
